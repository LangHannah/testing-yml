name: testing-yaml

on:
  schedule:
    - cron: '1 * * * *'
  workflow-dispatch: 
    
jobs: 
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo: "hello world"
